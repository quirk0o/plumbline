@AGENTS.md

Never use `cd` in terminal commands. Always run commands from your working directory and specify the path to the file directly. This ensures consistent context and prevents errors from running commands in the wrong directory.
